#include <stdio.h>
#include <string.h>

/*
int main () {
   char str[100] = "B1u 1bir 1deneme 1yazısıdır.";
   const char delim[2] = "1";
   char *token;

   token = strtok(str, delim);

   while( token != NULL ) {
      printf( "%s\n", token );
      token = strtok(NULL, delim);
   }

   return 0;
}
*/

/*
int main(void)
{
    char str[] = "Jesus loves you";
    char *delim = " ";
    char *token;

    token = strtok(str, delim);
    
    while (token)
    {
        printf("%s\n", token);
        token = strtok(NULL, delim);
    }
    return (0);
}
*/
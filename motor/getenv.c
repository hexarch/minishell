#include <stdio.h>
#include <stdlib.h>

/*
int main() {
    char *home_dir = getenv("PWD");
    if (home_dir == NULL) {
        printf("PWD ortam değişkeni bulunamadı.\n");
    } else {
        printf("PWD: %s\n", home_dir);
    }
    return 0;
}
*/
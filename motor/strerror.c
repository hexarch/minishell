#include <stdio.h>
#include <string.h>
#include <errno.h>

/*
int main() {
    for (int i = 0; i < 100; i++) {
        printf("%d: %s\n", i, strerror(i));
    }
    return 0;
}
*/

/*
int main() 
{

    FILE *fp = fopen("yanlis.txt", "r");

    if (fp == NULL) {
        printf("Hata: %s\n", strerror(errno));
        return 1;
    }
    // Dosya işlemleri yapılır
    fclose(fp);
    return 0;
}
*/
#include <stdio.h>
#include <unistd.h>

/*
int main() {
    int term_in, term_out;

    term_in = isatty(0);
    if (term_in) {
        printf("stdin bir terminal ile ilişkili.\n");
    } else {
        printf("stdin bir terminal ile ilişkili değil.\n");
    }

    term_out = isatty(1);
    if (term_out) {
        printf("stdout bir terminal ile ilişkili.\n");
    } else {
        printf("stdout bir terminal ile ilişkili değil.\n");
    }

    return 0;
}
*/